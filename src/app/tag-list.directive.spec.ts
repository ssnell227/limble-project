import { TagListDirective } from './tag-list.directive';

describe('TagListDirective', () => {
  it('should create an instance', () => {
    const directive = new TagListDirective();
    expect(directive).toBeTruthy();
  });
});
